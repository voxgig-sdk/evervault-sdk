# Entity: bin_lookup

main: kit: entity: bin_lookup: {

  alias: field: {}

  fields: [
    {
      name: "number"
      req: true
      short: "The card number for which the BIN lookup is being requested."
      type: "`$STRING`"
    }
  ]

  name: "bin_lookup"
  op: {
    create: {
      name: "create"
      points: [
        {
          method: "POST"
          orig: "/payments/bin-lookups"
          parts: [
            "payments"
            "bin-lookups"
          ]
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
